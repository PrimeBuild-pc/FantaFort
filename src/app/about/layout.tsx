import type { Metadata } from 'next';
export const metadata:Metadata={title:'About FantaFort Data and Rules',description:'How FantaFort uses competitive Fortnite results, its independent status, data sources and sandbox rules.',alternates:{canonical:'/about'}};
export default function Layout({children}:{children:React.ReactNode}){return children}
