import type { Metadata } from 'next';

export const metadata:Metadata={title:'FNCS Tournaments',description:'Authenticated FantaFort tournament schedules, scoring, leaderboards and match history.',robots:{index:false,follow:false}};
export default function Layout({children}:{children:React.ReactNode}){return children}
