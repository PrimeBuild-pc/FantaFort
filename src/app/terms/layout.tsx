import type { Metadata } from 'next';

export const metadata:Metadata={title:'Terms of Use',description:'Terms for participating in the free, limited FantaFort public alpha.',alternates:{canonical:'/terms'}};
export default function Layout({children}:{children:React.ReactNode}){return children}
