import type { Metadata } from 'next';

export const metadata:Metadata={title:'Support',description:'Support, privacy and private security contacts for the FantaFort public alpha.',alternates:{canonical:'/support'}};
export default function Layout({children}:{children:React.ReactNode}){return children}
