import type { Metadata } from 'next';

export const metadata:Metadata={title:'Privacy Policy',description:'How FantaFort processes and protects personal data during its free public alpha.',alternates:{canonical:'/privacy'}};
export default function Layout({children}:{children:React.ReactNode}){return children}
