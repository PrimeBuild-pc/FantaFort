import type { Metadata } from 'next';
export const metadata:Metadata={title:'Cookie and Storage Notice',description:'Technical cookies and local storage used by FantaFort for authentication and preferences.',alternates:{canonical:'/cookies'}};
export default function Layout({children}:{children:React.ReactNode}){return children}
